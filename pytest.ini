[pytest]
# The real test suite lives in tests/. scripts/test_notion.py is a manual
# verification script (run directly), not a pytest test, so keep collection
# scoped to tests/ and don't recurse into scripts/.
testpaths = tests
norecursedirs = scripts .venv frontend frontend-react node_modules
